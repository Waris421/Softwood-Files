import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Marketing',
  description: 'Marketing',
}

const page = () => {
    return (
        <div>
        </div>
    )
}

export default page
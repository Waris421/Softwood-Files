import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Procurement',
  description: 'Procurement',
}

const page = () => {
    return (
        <div>
        </div>
    )
}

export default page